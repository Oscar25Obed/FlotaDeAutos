export interface Vehicle {
    licensePlate: string;
    model: string;
    year: number;
    status: string;
    mileage: number; // en kilómetros
    fuelConsumption: number; // en litros por cada 100 km
}